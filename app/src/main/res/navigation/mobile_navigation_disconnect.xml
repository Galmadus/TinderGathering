<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation_disconnect"
    app:startDestination="@id/navigation_login_disconnect">

    <fragment
        android:id="@+id/navigation_login_disconnect"
        android:name="com.example.tindergathering.ui.login.LoginFragment"
        android:label="@string/title_login"
        tools:layout="@layout/fragment_login" />
    <fragment
        android:id="@+id/navigation_registration_disconnect"
        android:name="com.example.tindergathering.ui.Registration.RegistrationFragment"
        android:label="@string/title_registration" />

</navigation>